import { writeFileSync } from 'node:fs';
import { join } from 'node:path';

const settingsPath = join(process.cwd(), 'android', 'capacitor.settings.gradle');
const content = `// DO NOT EDIT THIS FILE! IT IS GENERATED EACH TIME "capacitor update" IS RUN
include ':capacitor-android'
project(':capacitor-android').projectDir = new File('../node_modules/@capacitor/android/capacitor')

include ':capacitor-health'
project(':capacitor-health').projectDir = new File('../node_modules/capacitor-health/android')
`;

writeFileSync(settingsPath, content, 'utf8');
console.log('Patched android/capacitor.settings.gradle (stable node_modules paths)');
